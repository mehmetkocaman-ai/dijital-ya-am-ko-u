const GoalsSection = window.GoalsSection = ({ store }) => (
  <section className="relative z-10 rounded-[2rem] border-4 border-white bg-white p-5 shadow-[0_14px_0_#dcefd4] md:p-7 lg:col-span-2"><div className="mb-6 flex flex-col gap-2 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-black uppercase tracking-[0.2em] text-purple-500">Hedefler</p><h2 className="text-3xl font-black">Hedef Sistemi</h2></div><p className="font-extrabold text-slate-400">{store.completedGoals.length}/{store.coachState.goals.length} hedef tamamlandı • Başarı %{store.goalSuccessRate}</p></div><form onSubmit={store.saveGoal} className="mb-6 grid gap-4 lg:grid-cols-6"><input value={store.goalForm.title} onChange={(e) => store.setGoalForm({ ...store.goalForm, title: e.target.value })} placeholder="Hedef başlığı" className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300 lg:col-span-2" /><input value={store.goalForm.description} onChange={(e) => store.setGoalForm({ ...store.goalForm, description: e.target.value })} placeholder="Açıklama" className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300 lg:col-span-2" /><select value={store.goalForm.type} onChange={(e) => store.setGoalForm({ ...store.goalForm, type: e.target.value })} className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300">{goalTypes.map((goalType) => <option key={goalType.id} value={goalType.id}>{goalType.name}</option>)}</select><select value={store.goalForm.category} onChange={(e) => store.setGoalForm({ ...store.goalForm, category: e.target.value })} className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300">{taskCategories.map((category) => <option key={category.id} value={category.id}>{category.icon} {category.name}</option>)}</select><input type="number" min="1" value={store.goalForm.targetValue} onChange={(e) => store.setGoalForm({ ...store.goalForm, targetValue: e.target.value })} placeholder="Hedef değeri" className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300" /><input type="number" min="0" value={store.goalForm.currentProgress} onChange={(e) => store.setGoalForm({ ...store.goalForm, currentProgress: e.target.value })} placeholder="Mevcut ilerleme" className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300" /><input type="date" value={store.goalForm.startDate} onChange={(e) => store.setGoalForm({ ...store.goalForm, startDate: e.target.value })} className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300" /><input type="date" value={store.goalForm.endDate} onChange={(e) => store.setGoalForm({ ...store.goalForm, endDate: e.target.value })} className="rounded-2xl border-4 border-slate-100 px-4 py-3 font-bold outline-none focus:border-purple-300" /><button className="rounded-2xl bg-purple-400 px-5 py-3 font-black text-white shadow-[0_8px_0_#a855f7] lg:col-span-2">Hedef oluştur</button></form><div className="grid gap-4 lg:grid-cols-2">{store.activeGoals.map((goal) => { const progress = Math.min(Math.round((goal.currentProgress / goal.targetValue) * 100), 100); const goalType = getGoalType(goal.type); return <article key={goal.id} className={`rounded-[1.5rem] border-4 border-white bg-gradient-to-br p-5 ${goalType.color} shadow-[0_8px_0]`}><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-black uppercase tracking-[0.18em] opacity-70">{goalType.name}</p><h3 className="text-2xl font-black">{goal.title}</h3><p className="mt-1 text-sm font-bold opacity-80">{goal.description}</p></div><span className="rounded-full bg-white/80 px-3 py-2 text-sm font-black">{getCategory(goal.category).icon} {getCategory(goal.category).name}</span></div><div className="mt-4 h-4 overflow-hidden rounded-full bg-white/70"><div className="h-full rounded-full bg-lime-400 transition-all" style={{ width: `${progress}%` }} /></div><div className="mt-3 flex flex-wrap items-center justify-between gap-3 text-sm font-black"><span>{goal.currentProgress}/{goal.targetValue} • %{progress}</span><span>{goal.startDate} → {goal.endDate}</span><span>Ödül: +{goalType.xp} XP • 🪙 +{goalType.coins}</span></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={() => store.updateGoalProgress(goal.id, goal.currentProgress + 1)} className="rounded-xl bg-white px-4 py-2 text-sm font-black shadow-[0_4px_0_rgba(15,23,42,0.12)]">+1 ilerle</button><button onClick={() => store.completeGoal(goal)} className="rounded-xl bg-lime-400 px-4 py-2 text-sm font-black text-slate-900 shadow-[0_4px_0_#84cc16]">Tamamla</button></div></article>; })}</div><div className="mt-6 rounded-[1.5rem] bg-slate-100 p-5"><h3 className="text-2xl font-black text-slate-800">Tamamlanan hedefler</h3><div className="mt-4 grid gap-3 md:grid-cols-2">{store.completedGoals.map((goal) => <div key={goal.id} className="rounded-2xl bg-white p-4 font-bold text-slate-500 shadow-[0_6px_0_#e5e7eb]"><p className="text-lg font-black text-slate-800">✅ {goal.title}</p><p>{getGoalType(goal.type).name} • {getCategory(goal.category).icon} {getCategory(goal.category).name}</p><p>Ödül alındı: +{getGoalType(goal.type).xp} XP • 🪙 +{getGoalType(goal.type).coins}</p></div>)}{store.completedGoals.length === 0 && <p className="font-black text-slate-400">Henüz tamamlanan hedef yok.</p>}</div></div></section>
);
