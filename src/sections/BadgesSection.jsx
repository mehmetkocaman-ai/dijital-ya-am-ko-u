const BadgesSection = window.BadgesSection = ({ unlockedBadges, lifetimeXp }) => (
  <section className="relative z-10 rounded-[2rem] border-4 border-white bg-white p-5 shadow-[0_14px_0_#dcefd4] md:p-7 lg:col-span-2"><div className="mb-6 flex flex-col gap-2 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-black uppercase tracking-[0.2em] text-lime-500">Koleksiyon</p><h2 className="text-3xl font-black">Başarı Rozetleri</h2></div><p className="font-extrabold text-slate-400">{unlockedBadges.length}/{badgeDefinitions.length} rozet açıldı • Toplam {lifetimeXp} XP</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{badgeDefinitions.map((badge) => { const isUnlocked = unlockedBadges.includes(badge.id); return <article key={badge.id} className={`rounded-[1.5rem] border-4 p-4 transition ${isUnlocked ? `border-white bg-gradient-to-br ${badge.color} shadow-[0_8px_0]` : 'border-slate-200 bg-slate-100 text-slate-400 grayscale shadow-[0_8px_0_#e5e7eb]'}`}><div className="mb-4 flex items-center justify-between"><span className="text-4xl">{isUnlocked ? badge.icon : '🔒'}</span><span className={`rounded-full px-3 py-1 text-xs font-black ${isUnlocked ? 'bg-white/80' : 'bg-slate-200'}`}>{isUnlocked ? 'Açıldı' : 'Kilitli'}</span></div><h3 className="text-lg font-black">{badge.title}</h3><p className="mt-2 text-sm font-bold leading-6 opacity-80">{badge.description}</p></article>; })}</div></section>
);
